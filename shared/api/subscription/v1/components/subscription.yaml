type: object
properties:
  uuid:
    type: string
    format: uuid
  status:
    $ref: ./enums/subscription_status.yaml
  plan_id:
    type: string
  plan_name:
    type: string
  amount:
    type: integer
    format: int64
    description: Стоимость в копейках, берётся из тарифа
  currency:
    type: string
    description: Валюта, берётся из тарифа
  payment_method:
    $ref: ./enums/payment_method.yaml
  expires_at:
    type: string
    format: date-time
    description: Заполняется после оплаты